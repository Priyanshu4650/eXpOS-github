int main()
{
decl
	int temp,name;
enddecl
begin
	temp = exposcall ( "Write", -2,"Enter name of the program");
	temp = exposcall ( "Read", -1, name);
	temp = exposcall ( "Exec", name);
	if(temp == -1)then
		temp = exposcall ( "Write", -2, "Invalid");
	endif;
	return 0;
end
}
